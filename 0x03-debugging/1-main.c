#include <stdio.h>

/**
 * main - makes while loop run infinitely
 *
 * Return: always 0
 */

int main(void)
{
	printf("Infinite loop incoming :(\n");

	i = 0;

	/*
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\n");
	return (0);
}
