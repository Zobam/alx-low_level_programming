#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @i: number recieved as argument
 *
 * Return: returns the absolute value of an integer
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	return (-i);
}
